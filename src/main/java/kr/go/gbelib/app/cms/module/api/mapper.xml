<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.go.gbelib.app.cms.module.api.ApiLogDao">
	
	<insert id="addApiLog">
		<selectKey keyProperty="log_idx" order="BEFORE" resultType="int">
			SELECT NVL(MAX(log_idx),0)+1 FROM USER_API_LOG
		</selectKey>

		INSERT INTO USER_API_LOG (
			log_idx, api_mode, code, msg, params, user_ip, log_date
		) VALUES (
			#{log_idx}, #{api_mode}, #{code}, #{msg}, #{params}, #{user_ip}, SYSDATE
		)
	</insert>
	
</mapper>