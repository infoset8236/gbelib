<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.go.gbelib.app.cms.module.certLog.CertLogDao">
	
	<insert id="addLog">
		INSERT INTO CERT_LOG (
			log_idx, cert_mode, cert_type, name, birthday, cell_phone, ci, msg, ip, log_date
		) VALUES (
			CERT_LOG_SEQ.NEXTVAL, #{cert_mode}, #{cert_type}, #{name}, #{birthday}, #{cell_phone}, #{ci}, #{msg}, #{ip}, SYSDATE
		)
	</insert>
	
	<select id="getCertLogList" resultType="kr.go.gbelib.app.cms.module.certLog.CertLog">
		SELECT
			log_idx, cert_mode, cert_type, name, birthday, cell_phone, ci, msg, ip, TO_CHAR(log_date, 'YYYY-MM-DD HH24:MI:SS') AS log_date
		FROM
			CERT_LOG
		ORDER BY log_idx DESC
	</select>
	
</mapper>