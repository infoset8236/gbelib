<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.go.gbelib.app.cms.module.dlscMember.DlscMemberDao">
	
	<select id="getDlscMember" resultType="kr.go.gbelib.app.cms.module.dlscMember.DlscMember">
		${pagingTop}
			SELECT
				dls_member_idx, dls_id, lib_id, web_id, user_name,
				user_ip, add_date 
			FROM 
				DLS_MEMBER
			<if test=" search_type != null and search_type != '' and search_text != '' and search_text != '' "> 
			WHERE	${search_type} LIKE '%'||#{search_text}||'%'
			</if>
			ORDER BY 
				${sortField} ${sortType}
		${pagingBottom}
	</select>
	
	<select id="getDlscMemberCount" resultType="int">
		SELECT
			COUNT(*) 
		FROM 
			DLS_MEMBER
		<if test=" search_type != null and search_type != '' and search_text != '' and search_text != '' "> 
		WHERE	${search_type} LIKE '%'||#{search_text}||'%'
		</if>
	</select>
	
</mapper>